namespace TypingGame.Mech
{
    public enum Difficulty
    {
        EASY,
        MEDIUM,
        HARD
    }

    public enum HealthPoint
    {
        ONE,
        TWO, 
        THREE
    }

    public enum State
    {
        CURRENT,
        CORRECT,
        INCORRECT,
        TBT
    }
}